/**
 * Animations barrel export
 * =========================
 * Shared animation variants and utilities for page/section transitions.
 *
 * Future exports (when Framer Motion is added):
 * export { fadeIn, slideUp, stagger } from "./variants";
 * export { PageTransition } from "./page-transition";
 *
 * Future exports (when Three.js / R3F is added):
 * export { useOrbitAnimation } from "./three/orbit";
 * export { useParticleAnimation } from "./three/particles";
 */
