/**
 * Constants barrel export
 * =======================
 * Re-exports all constants from a single entry point.
 * Usage: import { SITE_CONFIG, EASING } from "@/constants";
 */

export { SITE_CONFIG, NAV_ITEMS, BREAKPOINTS } from "./site";
export { EASING, DURATION, ANIMATION_CSS_VARS } from "./animations";
